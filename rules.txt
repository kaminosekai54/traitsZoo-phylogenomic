import os

rule convertToTBLAndFasta:

    fileNames = [file.replace(".pep","") for file in os.listdir("rawData") if file.endswith(".pep")]
    input:
        expand("{dataset}/{name}.{ext}", dataset="rawData", name=fileNames, ext="pep")
    output:
        expand("{dataset}/{name}.{ext}", dataset="data/tbl_files", name=fileNames, ext="tsv")
        expand("{dataset}/{name}.{ext}", dataset="data/fasta_files", name=fileNames, ext="fasta")
    shell:
        "python3 fastaRenamer.py {input}"
